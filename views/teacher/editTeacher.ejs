<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>Page Title</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css"> 
</head>
<body>
    <h1>Edit Data Teacher</h1>
    <% if(message !== null ||message !== undefined){ %>
        <%= message %>
    <% } %>
        <div class="w3-container w3-blue">
                <h2>Input Form</h2>
              </div>
              
              <form class="w3-container" action="/teacher/edit/<%= dataToEdit.id %>" method="POST">
                <p>
                <label>First Name</label>
                <input class="w3-input" type="text" value="<%= dataToEdit.firstName %>" name="firstName"></p>
                <p>
                <label>Last Name</label>
                <input class="w3-input" type="text" value="<%= dataToEdit.lastName %>" name="lastName"> </p>
                <p>
                <label>Email</label>
                <input class="w3-input" type="text" value="<%= dataToEdit.email %>" name="email"></p>

                <label for="subject"> Subject</label>
                <select id="subject" name="SubjectId">
                        <% dataSubjects.forEach(dataSubject =>{ %>
                        <option name="SubjectId" value="<%= dataSubject.id%>"><%= dataSubject.subjectName%></option>
                        <% }) %>
                    </select>
              
                <input type="submit" value="Submit">
              </form>
</body>
</html>